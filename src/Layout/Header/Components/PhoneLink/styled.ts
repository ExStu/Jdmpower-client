import { styled } from "@mui/material/styles";

export const SPhoneLinkWrap = styled("div")(() => ({
  display: "flex",
  flexDirection: "column",
}));

export const SPhonePlaceText = styled("span")(() => ({
  textTransform: "none",
}));
