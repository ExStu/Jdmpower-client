"use client";

import { styled } from "@mui/material/styles";

export const SCart = styled("section")(() => ({
  padding: "60px 0",
}));

export const SCartActions = styled("div")(() => ({
  position: "sticky",
  top: "20px",
  display: "flex",
  flexDirection: "column",
  gap: "20px",
}));
