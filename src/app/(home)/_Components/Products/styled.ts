import { styled } from "@mui/material/styles";

export const SProducts = styled("section")(({ theme: { palette } }) => ({
  paddingTop: "60px",
  paddingBottom: "100px",
  backgroundColor: palette.uncategorized.background,
}));
